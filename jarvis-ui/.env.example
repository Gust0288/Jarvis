# Optional text-to-speech provider key.
# Used only when TTS_BACKEND is set to "openai" or "elevenlabs" in src/App.jsx.
VITE_TTS_API_KEY=

# Optional FreeLLMAPI unified key from your local freellmapi dashboard/server log.
VITE_FREELLM_KEY=
